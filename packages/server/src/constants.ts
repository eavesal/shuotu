import { resolve } from 'path'

export const DATA_FOLDER = resolve(__dirname, '../data')
export const BILL_CSV_FILE_PATH = resolve(DATA_FOLDER, 'bill.csv')
export const CATEGORIES_CSV_FILE_PATH = resolve(DATA_FOLDER, 'categories.csv')
